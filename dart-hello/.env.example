# =============================================================================
# dart-hello — Environment Variables (.env.example)
# =============================================================================
# Factor 3: Store config in the environment

# --- Application ---
SERVICE_NAME=loan-service
SERVICE_VERSION=1.2.0
SERVICE_ENV=production
LOG_LEVEL=info

# --- Serverpod ---
runmode=production
serverid=default
logging=normal
role=monolith
